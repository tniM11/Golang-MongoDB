version: '3.8'
services:
  mongo:
    image: mongo:7.0
    container_name: todos-mongo
    ports:
      - "27017:27017"
    volumes:
      - ./scripts/init-db.js:/docker-entrypoint-initdb.d/init-db.js:ro
